{
  "name": "yasiman",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node backend/src/index.js",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "heroku-postbuild": "npm install && npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "debug": "^4.3.6"
  },
  "engines": {
    "node": "18.x"
  }
}
